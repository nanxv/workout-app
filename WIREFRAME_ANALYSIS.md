# Wireframe v1.8.3 分析与实现计划

## 核心设计特点

### 1. 训练页面 (TrainScreen)
**关键特性**：
- ✅ 状态胶囊栏（已实现 StatusCapsuleView）
- ✅ Day 卡片可展开/折叠（已实现 RoutineCardView）
- ⚠️ **需要改进**：动作详情展开显示所有组，支持勾选完成
- ⚠️ **需要新增**：悬浮球（可拖拽，显示当前训练状态）
- ⚠️ **需要改进**：组记录支持次数/时长/重量三种模式

**当前实现 vs Wireframe**：
- ✅ 状态胶囊：已实现，可折叠
- ✅ Day 卡片：已实现，可展开
- ❌ 动作详情：当前是简单列表，需要改为可展开的组记录
- ❌ 悬浮球：未实现，需要新增
- ❌ 组记录：当前只有 reps/RIR，需要支持 holdSec 和 weightKg

### 2. 计划页面 (RoutinesScreen)
**关键特性**：
- ✅ Day 卡片可编辑（已实现）
- ✅ 动作行可编辑（已实现）
- ⚠️ **需要改进**：行内编辑控件（InlineEditText/Number）
- ⚠️ **需要新增**：动作排序（上移/下移）
- ⚠️ **需要新增**：动作复制功能
- ⚠️ **需要新增**：导入/导出 JSON

**当前实现 vs Wireframe**：
- ✅ Day 卡片：已实现
- ✅ 动作编辑：已实现基本编辑
- ❌ 行内编辑：当前是表单，需要改为行内编辑
- ❌ 排序/复制：未实现
- ❌ 导入/导出：未实现

### 3. 记录页面 (RecordsScreen)
**关键特性**：
- ✅ 筛选（已实现 HistoryFilter）
- ✅ 历史列表（已实现）
- ⚠️ **需要新增**：概览卡片（本周时长、训练量）
- ⚠️ **需要新增**：亮点展示
- ⚠️ **需要改进**：列表项显示更丰富的信息

**当前实现 vs Wireframe**：
- ✅ 筛选：已实现
- ✅ 列表：已实现
- ❌ 概览卡片：未实现
- ❌ 亮点：未实现

### 4. 教练页面 (CoachScreen)
**关键特性**：
- ✅ 聊天界面（已实现）
- ⚠️ **需要改进**：限制显示最近 3 条消息
- ⚠️ **需要新增**：快捷命令按钮
- ⚠️ **需要改进**：极简风格，单屏显示

**当前实现 vs Wireframe**：
- ✅ 聊天：已实现
- ❌ 消息限制：未实现
- ❌ 快捷按钮：未实现

### 5. 设置页面 (SettingsScreen)
**关键特性**：
- ❌ **需要新增**：完整的设置页面
- ⚠️ **需要新增**：显示选项（状态胶囊、紧凑列表）
- ⚠️ **需要新增**：数据管理（恢复默认、导出/导入）

**当前实现 vs Wireframe**：
- ❌ 设置页面：未实现

### 6. 底部导航 (BottomNav)
**关键特性**：
- ✅ Tab 导航（已实现 MainTabView）
- ⚠️ **需要改进**：中间"训练"按钮凸起设计
- ⚠️ **需要改进**：5 个标签等宽布局

**当前实现 vs Wireframe**：
- ✅ Tab：已实现
- ❌ 凸起按钮：未实现
- ❌ 等宽布局：未实现

## 实现优先级

### P0 - 核心训练体验
1. **悬浮球组件** - 可拖拽，显示当前训练状态
2. **动作详情展开** - 显示所有组，支持勾选完成
3. **组记录改进** - 支持次数/时长/重量三种模式

### P1 - 计划管理增强
4. **行内编辑控件** - 点击即编辑
5. **动作排序/复制** - 上移/下移/复制功能
6. **导入/导出 JSON** - 数据备份和恢复

### P2 - 页面完善
7. **记录页概览卡片** - 本周统计和亮点
8. **教练页快捷按钮** - 常用命令快速访问
9. **设置页面** - 完整的设置界面
10. **底部导航优化** - 凸起训练按钮

## 技术实现要点

### 悬浮球实现
- 使用 `DragGesture` 实现拖拽
- 使用 `GeometryReader` 限制拖拽范围
- 使用 `@State` 保存位置（可持久化到 UserDefaults）
- 贴边吸附逻辑

### 行内编辑实现
- 使用 `@State` 控制编辑状态
- 点击切换为 TextField
- Enter 确认，Escape 取消
- 失焦自动提交

### 组记录三种模式
- 扩展 `SetEntry` 模型：添加 `holdSec` 和 `weightKg`
- UI 根据模式显示不同输入控件
- 次数模式：数字输入 + 快捷按钮（+1/+5/-1）
- 时长模式：秒数输入
- 重量模式：可选重量输入

